datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Mint {
  id        String   @id @default(uuid())
  eventId   String   @unique
  to        String
  amount    String
  ledger    Int
  txHash    String
  createdAt DateTime @default(now())
}

model Transfer {
  id        String   @id @default(uuid())
  eventId   String   @unique
  from      String
  to        String
  amount    String
  ledger    Int
  txHash    String
  createdAt DateTime @default(now())
}

model Burn {
  id        String   @id @default(uuid())
  eventId   String   @unique
  from      String
  amount    String
  ledger    Int
  txHash    String
  createdAt DateTime @default(now())
}

model LastIndexedLedger {
  id     Int @id @default(1)
  ledger Int
}

